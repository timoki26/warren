# AUTO-GENERATED by `bun run gen:openapi` from `src/server/handlers/route-table.ts`.
# Do not edit by hand. CI fails if this file is out of sync.
openapi: 3.1.0
info:
  title: warren HTTP API
  version: 0.14.1
  description: >-
    Auto-generated from `src/server/handlers/route-table.ts`'s `ROUTE_TABLE`. Run `bun run
    gen:openapi` to refresh; CI fails if this schema drifts from the handler module.
    Request/response bodies are intentionally left permissive in V1 — see docs/http-api.md for the
    canonical handler contracts.
tags:
  - name: agents
    description: '`/agents` routes.'
  - name: alerts
    description: '`/alerts` routes.'
  - name: analytics
    description: '`/analytics` routes.'
  - name: api
    description: '`/api` routes.'
  - name: github-app
    description: '`/github-app` routes.'
  - name: meta
    description: Liveness, readiness, and version.
  - name: metrics
    description: '`/metrics` routes.'
  - name: plan-runs
    description: '`/plan-runs` routes.'
  - name: preview
    description: '`/preview` routes.'
  - name: projects
    description: '`/projects` routes.'
  - name: runs
    description: '`/runs` routes.'
  - name: whoami
    description: '`/whoami` routes.'
paths:
  /agents:
    get:
      operationId: listAgentsHandler
      tags:
        - agents
      summary: GET /agents
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /agents/{name}:
    get:
      operationId: getAgentHandler
      tags:
        - agents
      summary: GET /agents/:name
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
  /alerts/heal:
    post:
      operationId: healAlertHandler
      tags:
        - alerts
      summary: POST /alerts/heal
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: >-
        warren-3db0: closed-loop alert intake. Token-gated via the standard bearer gate (not
        auth-exempt); webhook senders carry the bearer.
  /analytics/behavior:
    get:
      operationId: listBehaviorAnalyticsHandler
      tags:
        - analytics
      summary: GET /analytics/behavior
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /analytics/cost:
    get:
      operationId: listCostAnalyticsHandler
      tags:
        - analytics
      summary: GET /analytics/cost
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /analytics/runs:
    get:
      operationId: listRunAnalyticsHandler
      tags:
        - analytics
      summary: GET /analytics/runs
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /api/sample-greeting:
    get:
      operationId: sampleGreetingHandler
      tags:
        - api
      summary: GET /api/sample-greeting
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /github-app/callback:
    get:
      operationId: gitHubAppCallbackHandler
      tags:
        - github-app
      summary: GET /github-app/callback
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /github-app/register:
    get:
      operationId: registerGitHubAppHandler
      tags:
        - github-app
      summary: GET /github-app/register
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: 'warren-a647: App manifest registration; anonymous — see ./github-app.ts.'
  /healthz:
    get:
      operationId: healthzHandler
      tags:
        - meta
      summary: GET /healthz
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /metrics:
    get:
      operationId: metricsHandler
      tags:
        - metrics
      summary: GET /metrics
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /plan-runs:
    get:
      operationId: listPlanRunsHandler
      tags:
        - plan-runs
      summary: GET /plan-runs
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
    post:
      operationId: createPlanRunHandler
      tags:
        - plan-runs
      summary: POST /plan-runs
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /plan-runs/{id}:
    get:
      operationId: getPlanRunHandler
      tags:
        - plan-runs
      summary: GET /plan-runs/:id
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plan-runs/{id}/cancel:
    post:
      operationId: cancelPlanRunHandler
      tags:
        - plan-runs
      summary: POST /plan-runs/:id/cancel
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plan-runs/{id}/events:
    get:
      operationId: streamPlanRunEventsHandler
      tags:
        - plan-runs
      summary: GET /plan-runs/:id/events
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /preview/config:
    get:
      operationId: previewConfigHandler
      tags:
        - preview
      summary: GET /preview/config
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /projects:
    get:
      operationId: listProjectsHandler
      tags:
        - projects
      summary: GET /projects
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
    post:
      operationId: createProjectHandler
      tags:
        - projects
      summary: POST /projects
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /projects/{id}:
    get:
      operationId: getProjectHandler
      tags:
        - projects
      summary: GET /projects/:id
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
    delete:
      operationId: deleteProjectHandler
      tags:
        - projects
      summary: DELETE /projects/:id
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/ready-plans:
    get:
      operationId: listReadyPlansHandler
      tags:
        - projects
      summary: GET /projects/:id/ready-plans
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/refresh:
    post:
      operationId: refreshProjectHandler
      tags:
        - projects
      summary: POST /projects/:id/refresh
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/seeds/plans:
    get:
      operationId: listProjectSeedPlansHandler
      tags:
        - projects
      summary: GET /projects/:id/seeds/plans
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: >-
        Static path — must precede `/projects/:id/seeds/:seedId` so the param route doesn't swallow
        `plans` as a seed id.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/seeds/{seedId}:
    get:
      operationId: getProjectSeedHandler
      tags:
        - projects
      summary: GET /projects/:id/seeds/:seedId
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: seedId
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/triggers:
    get:
      operationId: getProjectTriggersHandler
      tags:
        - projects
      summary: GET /projects/:id/triggers
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/triggers/{triggerId}/run:
    post:
      operationId: runProjectTriggerHandler
      tags:
        - projects
      summary: POST /projects/:id/triggers/:triggerId/run
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: triggerId
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/warren-config:
    get:
      operationId: getProjectWarrenConfigHandler
      tags:
        - projects
      summary: GET /projects/:id/warren-config
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /readyz:
    get:
      operationId: readyzHandler
      tags:
        - meta
      summary: GET /readyz
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /runs:
    get:
      operationId: listRunsHandler
      tags:
        - runs
      summary: GET /runs
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
    post:
      operationId: createRunHandler
      tags:
        - runs
      summary: POST /runs
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /runs/{id}:
    get:
      operationId: getRunHandler
      tags:
        - runs
      summary: GET /runs/:id
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/cancel:
    post:
      operationId: cancelRunHandler
      tags:
        - runs
      summary: POST /runs/:id/cancel
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/events:
    get:
      operationId: streamRunEventsHandler
      tags:
        - runs
      summary: GET /runs/:id/events
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: >-
        NDJSON event tail. `?follow=1` live-tails (the default while the run is non-terminal);
        `?limit=N` requests a bounded non-streaming read of at most N events and implies
        follow=false — the response closes after the page, so agents can poll for liveness without
        holding a stream open (warren-17c1). `?since=<seq>` pages forward from a prior read.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/finalize-intent:
    get:
      operationId: getRunFinalizeIntentHandler
      tags:
        - runs
      summary: GET /runs/:id/finalize-intent
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: >-
        warren-0d35: the in-pod finalize callback for the K8s backend — the pod fetches the reap
        intent, runs the workspace-dependent half in place, and POSTs the FinalizeResult back.
        Bearer-gated; the pod carries its per-run scoped token (warren-57fd).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/finalize-result:
    post:
      operationId: postRunFinalizeResultHandler
      tags:
        - runs
      summary: POST /runs/:id/finalize-result
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/git-credential:
    post:
      operationId: postRunGitCredentialHandler
      tags:
        - runs
      summary: POST /runs/:id/git-credential
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: >-
        warren-c9ac: the in-pod credential re-mint (forge-contract.md §4.1 window 3) — the pod
        requests a freshly-minted push credential over the same authenticated callback channel
        instead of trusting the mounted Secret.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/inbox:
    get:
      operationId: pollRunInboxHandler
      tags:
        - runs
      summary: GET /runs/:id/inbox
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: >-
        warren-3d0b: the in-pod steering poll for the K8s backend. Bearer-gated like every /runs
        route; the pod carries its per-run SCOPED token (warren-57fd). Destructive on read (it
        claims unread messages), so for a non-run caller it is operator-only (warren-b875).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/preview/login:
    post:
      operationId: previewLoginHandler
      tags:
        - runs
      summary: POST /runs/:id/preview/login
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: >-
        warren-e1b0: POST, not GET — the bearer rides the `Authorization` header like every other
        /runs route instead of a `?token=` query string that would land in history / Referer / proxy
        logs.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/preview/teardown:
    post:
      operationId: previewTeardownHandler
      tags:
        - runs
      summary: POST /runs/:id/preview/teardown
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/salvage:
    post:
      operationId: postRunSalvageHandler
      tags:
        - runs
      summary: POST /runs/:id/salvage
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: >-
        warren-cd3b: the in-pod salvage intake — the pod POSTs the work it captured (rescue ref +
        git bundle) when the finalize branch push failed or no reap intent ever arrived, BEFORE its
        emptyDir dies with the pod.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/steer:
    post:
      operationId: steerRunHandler
      tags:
        - runs
      summary: POST /runs/:id/steer
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /version:
    get:
      operationId: versionHandler
      tags:
        - meta
      summary: GET /version
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /whoami:
    get:
      operationId: whoamiHandler
      tags:
        - whoami
      summary: GET /whoami
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: 'warren-e195: `readPublic`, not `anonymous` — an exempt route gets no actor to name.'
